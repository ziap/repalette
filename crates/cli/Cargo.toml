[package]
name = "repalette"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
image = "0.25.10"
png = "0.18.1"
repalette-core = { path = "../core" }
repalette-palettes = { path = "../palettes" }
