[package]
name = "repalette"
version = "0.1.0"
edition = "2024"

[build-dependencies]
cc = "1.2.63"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
image = "0.25.10"

[profile.release]
strip = true
lto = true
panic = "abort"
